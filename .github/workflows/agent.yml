name: AI Automation Test

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test automation
        run: |
          echo "GitHub AI automation is working!"
          echo "Repository: ${{ github.repository }}"
          echo "Commit: ${{ github.sha }}"
